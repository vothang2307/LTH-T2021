        public int UCLN(int a, int b)
        {
            a=Math.abs(a);
            b=Math.abs(b);
            while(a!=b && a!=0)
            {
              if(a>b) a=a-b;
              else b= b-a;
            }
            return PS;
        }
